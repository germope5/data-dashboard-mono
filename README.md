# Data Dashboard Monorepo

Monorepo demo: backend (Node + TypeScript + Express) and frontend (React + TypeScript + Vite).

Run:

```powershell
pnpm install
pnpm --filter backend dev
pnpm --filter frontend dev
```
